dependencies {
	compile project(":modules:blogs:blogs-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../blogs-api/src/main/java"
}

group = "blogs"